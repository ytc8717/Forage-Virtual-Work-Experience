import React from "react";
// const http = require("http");

function FetchIP({ip_type}) {
    // let url;
    // if(ip_type === "ipv4") {
    //     url = "api.ipify.org?format=json";
    // } else if(ip_type === "ipv6") {
    //     url = "api64.ipify.org?format=json"
    // } else {
    //     console.log("Invalid IP type: " + ip_type);
    //     return;
    // }
    // let userIP;
    // http.get({'host': {url}, 'port': 80, 'path': '/'}, function(resp) {
    //     resp.on('data', function(ip) {
    //         userIP = ip;
    //       //console.log("My public IP address is: " + ip);
    //     });
    //   });
    return (
        <></>
        // <div>{userIP}</div>
    );
}

export default FetchIP;